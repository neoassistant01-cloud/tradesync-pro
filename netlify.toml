[build]
  publish = "."
  command = "echo 'Static site - no build required'"

[dev]
  command = "python3 -m http.server 8000"
  port = 8000
  targetPort = 8000

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
